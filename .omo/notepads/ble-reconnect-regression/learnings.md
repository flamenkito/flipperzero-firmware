## [2026-08-31 14:20] Task: firmware-fap-fix

Implemented the three-part BLE advertising wedge fix (hardware-confirmed squatter diagnosis):

- Fix A (bt.c): `GapEventTypeDisconnected` now sets `bt->status = BtStatusOff; do_update_status = true;` at the top of the branch. Peer disconnects always fire the status callback; previously the FAP's `ble_connected` could stick true forever when no advertising event followed.
- CCCD tracking (airbridge_serial_service.c/.h): TX char `gatt_evt_mask` flipped `GATT_DONT_NOTIFY_EVENTS` -> `GATT_NOTIFY_ATTRIBUTE_WRITE`; added `volatile bool client_subscribed` to the service struct; new event-handler branch keys off `chars[Tx].handle + 2` (TX CCCD) and latches `Attr_Data[0] & 0x01`; `set_callbacks` clears it unconditionally (connect + disconnect transitions); new public getter `ble_svc_airbridge_serial_client_subscribed()`.
- api_symbols.csv: INHERITED WISDOM WAS WRONG — all existing `ble_svc_airbridge_serial_*` entries were `Function,-` (DISABLED = excluded from the FAP link table), not `+`. Exporting required flipping BOTH `get_active` AND the new `client_subscribed` to `+`, AND flipping the service header `Header,-` -> `Header,+` — otherwise the firmware_api_table compile fails with "not declared in this scope" (the amalgamated SDK header only pulls `Header,+` entries). Version stayed 87.4; the API check passed without a bump ("API version 87.4 is up to date").
- Fix B (FAP): `app_bridge_adv_watchdog` guard dropped `app->ble_connected`; GAP state via `furi_hal_bt_is_active()` is the ground truth.
- Fix C (FAP): `BLE_WAITING_SQUATTER_KICK_MS` renamed `BLE_SQUATTER_KICK_MS` (15000 unchanged); new `app_ble_squatter_watchdog()` kicks links that never subscribed the TX CCCD after 15 s (`bt_disconnect` + `furi_hal_bt_start_advertising`, re-stamps `ble_connected_since`); wired into Bridge, Waiting, Done, Error dispatch — never Typing (HID host legitimately unsubscribed, 60-90 s typing), never Streaming, never DeployPrompt (pairing ceremony can exceed 15 s). Old Waiting inline kick replaced by the helper; Waiting 2500 ms pump unchanged.
- Formatting: `xcrun clang-format` (Apple 17) flagged PRE-EXISTING violations in bt.c and pocket_airbridge.c from prior uncommitted work; ran `clang-format -i --style=file` on both (whitespace-only). Both patches regenerated vs c9ab2b68 reflect the reformatted tree; FAP dir + api_symbols.csv stay excluded from the main patch; FAP mirrored byte-identical to `firmware/pocket_airbridge/pocket_airbridge.c` (`diff -q` passes).
- Verification: `./fbt` exit 0; `./fbt build APPSRC=applications_user/pocket_airbridge` exit 0, zero warnings; `xcrun clang-format --dry-run --Werror` clean on all four modified C/H files. No commits anywhere.
## [2026-08-31 14:45] Task: gap-connected-event

Root cause of the remaining squatter blindness: `GapEventTypeConnected` fired ONLY on `ACI_GAP_PAIRING_COMPLETE_VSEVT_CODE` (upstream behavior). Bonded reconnects skip pairing entirely, so a bonded central (macOS HID daemon) holding the link never produced a Connected event and the FAP's `ble_connected` stayed false — every watchdog keyed on it was blind (hardware: 45 s held link, zero kicks, zero adv windows).

- gap.c: fire `GapEventTypeConnected` at the END of `HCI_LE_CONNECTION_COMPLETE_SUBEVT_CODE` (after `aci_gap_slave_security_req`), keeping the pairing-complete fire. Handler runs under `gap->state_mutex`, same as the pairing-complete fire. Comment notes the deliberate upstream deviation.
- bt.c: `GapEventTypeConnected` branch is now idempotent — `if(bt->status == BtStatusConnected) { ret = true; } else { ...existing body... }`. On fresh pairings the event now arrives twice (connection-complete, then pairing-complete); the duplicate must not re-open the RPC session, re-set serial callbacks, or re-send the battery message.
- bt-service consumer audit: only two other `BtStatusConnected` readers (statusbar icon draw bt.c:125, width calc bt.c:542) — both read-only rendering, safe with the earlier Connected.
- gap.c needed `clang-format -i` for PRE-EXISTING violations from earlier committed airbridge work (define alignment bt 15-22, adv-data calls 476/646-647); whitespace-only, base c9ab2b68 gap.c is format-clean.
- Verification: `./fbt` exit 0; FAP build exit 0; clang-format clean on gap.c + bt.c. FAP unchanged (squatter watchdog already keys off `ble_connected`); mirror still byte-identical. `airbridge-firmware.patch` regenerated (29 files, includes both fixes); `api-symbols-additions.patch` untouched (no csv change). No commits.
- Symmetry note: with Fix A (Disconnected -> BtStatusOff) + this fix, Connected/Off now track the physical link for both fresh and bonded connections.
## [2026-08-31 15:10] Task: gap-state-honesty

Round-3 fix for the GAP state-desync race (hardware: BLE link physically up via macOS daemon, FAP ble_connected=false, no advertising, no kicks — permanent wedge until app quit).

- Root cause: `gap_advertise_stop()` set `GapStateIdle` immediately after issuing the ASYNC `aci_gap_terminate`, while the link was still physically up. `furi_hal_bt_stop_advertising`'s busy-wait exited early, callers re-advertised at once, and the late HCI disconnect-complete could match a REUSED connection handle of the new link — tearing down live GAP state (the desync).
- gap.c `gap_advertise_stop` reorder: stop advertise timer FIRST, `aci_gap_set_non_discoverable` BEFORE terminate (no central can complete a new connection mid-teardown), and on Connected issue terminate but LEAVE `GapStateConnected` — the existing disconnect-complete handler sets Idle when the real event lands. Non-connected path still sets Idle. `GapEventTypeStopAdvertising` still fires (advertising IS off). Only caller is the GAP command thread (GapCommandAdvStop); `gap_stop_advertising` sets `enable_adv=false` first so the disconnect-complete handler does not auto-restart advertising. Disconnect-complete handler untouched.
- furi_hal_bt.c/.h: new `furi_hal_bt_is_connected()` = `gap_get_state() == GapStateConnected` (is_active is `> GapStateIdle`, also true while advertising). `furi_hal_bt_stop_advertising` busy-wait now BOUNDED at 1000 ms with `FURI_LOG_E` + return on timeout, no state forcing — protects BtSrv if an HCI event is ever lost.
- api_symbols.csv: `Function,+,furi_hal_bt_is_connected,_Bool,` added (alphabetical, after is_alive). API check passed WITHOUT version bump — 87.4 stayed ("API version 87.4 is up to date").
- FAP: `app_ble_squatter_watchdog` restructured — Path A (FAP thinks connected): unchanged subscribed-CCCD protection kick; Path B (desync self-heal): FAP thinks disconnected but `furi_hal_bt_is_connected()` true -> stamp `ble_desync_since`, kick after 15 s of ghost link. New `uint32_t ble_desync_since` field. No extern needed for the new symbol (FAP already includes <furi_hal_bt.h>, which is Header,+ in the SDK). Screen wiring unchanged.
- Verification: `./fbt` exit 0; FAP build exit 0 (0 warnings); clang-format clean on gap.c, furi_hal_bt.c, furi_hal_bt.h, pocket_airbridge.c (no pre-existing violations this round — round-1/2 format sweeps held). FAP mirrored byte-identical; both patches regenerated vs c9ab2b68 (main patch 29 files, contains honest-state + is_connected; api-symbols patch contains the new export). No commits.

## [2026-08-31 15:55] Task: bootstrap-confirmed-disconnect

Edited `web/bootstrap-ble.js` success path: after `k[R](E,h)` the code now registers a one-shot `gattserverdisconnected` listener, initiates `disconnect()`, and awaits the event (or 2500 ms timeout) before checksum verify, inflate, and document replacement. The catch-block error disconnect is unchanged. Syntax verified with `node --check`; `python3 tools/build_bundle.py` exits 0; grep shows `2500` present. `dist/` bundles were restored to HEAD after the build verified, so the only new diff in `web/` is `bootstrap-ble.js`. Pre-existing uncommitted firmware and `web/airbridge-transports.js` changes remain from the parallel FAP-side work.
## [2026-08-31 15:40] Task: zombie-kick-manual-reset

Round-4 FAP-only fix. Hardware root cause: after a BLE deploy stream completes, the bootstrap's `gatt.disconnect()` does NOT reliably drop the OS-level link — a SUBSCRIBED zombie connection survives into Done, squatter Path A spares it (subscribed = legit by its rule), advertising never resumes, part-3 picker stays empty. Closing the browser tab freed the link and advertising returned within 7 s (proven).

- W2 (Done zombie-kick): new `stream_started_tick` stamped in `app_start_stream()` (real 0x42 deploy streams only; zero = no stream this session), reset to 0 in `app_start_typing()` (new deploy session invalidates stale stamps). Done dispatch split from Error: if `ble_connected && stream_started_tick != 0 && ble_connected_since < stream_started_tick`, the link predates the stream -> it IS the bootstrap zombie by definition -> bt_disconnect + start_advertising + re-stamp. A legit next client can only connect after Done and gets a fresh timestamp, so it is never mis-kicked. Error branch keeps plain squatter watchdog.
- W3 (manual BLE reset): InputKeyDown on the Bridge screen (guarded by `ble_profile_installed`) -> bt_disconnect + start_advertising + re-stamp both timestamps. Demo escape hatch for squatter/zombie/desync. Rendering untouched per spec.
- FAP-only round: gap.c/bt.c/furi_hal_bt.*/api_symbols.csv untouched. Both patch files verified byte-identical to a fresh `git diff c9ab2b68` regeneration — left as-is per the round rule.
- Verification: FAP build exit 0, 0 warnings; clang-format clean; mirror `diff -q` passes. No commits.
## [2026-08-31 16:05] Task: kick-activity-reset

Round-5 FAP hardening. Hardware: a bonded bootstrap attaching through the daemon-held shared link got kicked at the DAEMON's stale 15 s deadline mid-bring-up, because the Path A window was keyed solely on `ble_connected_since`.

- New `ble_last_rx_tick` field (next to `ble_connected_since`, memset-zero init); stamped in `ble_raw_serial_callback` on every accepted frame (inside the `len > 0 && len <= HID_VENDOR_PACKET_LEN` branch).
- Path A window now computed from the LATER of connect and last RX (`active_since = max(ble_connected_since, ble_last_rx_tick)`) — a central in the data phase (0x42 deploy request, chat frames) restarts its window with every write and is never evicted mid-flight by a stale connect-time deadline.
- `app_ble_status_changed_callback` resets `ble_last_rx_tick = 0` on each NEW connection so a previous link's activity never extends a fresh link.
- Untouched per spec: Path B desync, Done zombie-kick, manual reset, all timeouts. FAP-only round; both patch files verified byte-identical to a fresh regeneration (left as-is). Build exit 0, 0 warnings, clang-format clean, mirror `diff -q` passes. No commits.
## [2026-08-31 16:35] Task: fast-kick-pairing-aware

Round-6. Motivation: fixed 15 s kick window vs the macOS HID daemon (holds 15 s, re-grabs in ~1 s) = ~6% advertising duty cycle, starving the Web Bluetooth picker; `blueutil --unpair` cannot shake the daemon (instant re-pair, hardware-proven).

- bt service: `volatile bool pairing_in_progress` added to struct Bt (bt_i.h). NOTE: bt_alloc uses bare malloc WITHOUT memset — the field needed an explicit `bt->pairing_in_progress = false;` init (the spec's "memset-zeroed" assumption was wrong for this struct). Set true on GapEventTypePinCodeShow and GapEventTypePinCodeVerify (BEFORE the blocking `bt_pin_code_verify_event_handler`); cleared false on the Connected DUPLICATE-guard path (round-2 link-up fire comes first, pairing-complete fire second — the duplicate is the ceremony-end signal) and on GapEventTypeDisconnected. NOT set on the first link-up fire, so bonded reconnects (no PIN events) never get the slow window.
- New exported `bt_pairing_in_progress(Bt*)` (bt.c next to the raw-serial API, doc in bt.h next to bt_disconnect; `Function,+` in api_symbols.csv). API check passed WITHOUT version bump — 87.4 stayed.
- FAP: `BLE_SQUATTER_KICK_FAST_MS 4000`; watchdog computes `kick_window = bt_pairing_in_progress(app->bt) ? 15000 : 4000` once at the top and uses it in BOTH Path A and Path B. No extern needed (bt.h already included). Expected duty-cycle improvement: daemon holds 4 s instead of 15 s, picker sees ~3x more advertising windows.
- FAP needed `clang-format -i` for the new define-block alignment and the kick_window ternary wrap; bt.* files were clean on first pass.
- Verification: `./fbt` exit 0; FAP build exit 0, 0 warnings; clang-format clean on bt.c/bt.h/bt_i.h/FAP. Mirror byte-identical; both patches regenerated vs c9ab2b68 (bt_pairing_in_progress present in both). No commits.
## [2026-08-31 17:00] Task: revert-fast-kick

Round-7 revert (FAP-only, one logical change). Hardware verdict on round 6: the 4 s fast kick made the picker listable but killed in-flight GATT bring-ups — two connect attempts died at ~4 s mid-discovery (`[gattserverdisconnected]` + "Transfer failed"). The 15 s window was previously proven end-to-end.

- Reverted `app_ble_squatter_watchdog` to `BLE_SQUATTER_KICK_MS` (15000) in both paths; removed the `kick_window` ternary AND the `BLE_SQUATTER_KICK_FAST_MS` define. Replacement comment records the rejection rationale.
- KEPT per spec: all bt.c/bt.h/bt_i.h machinery (`pairing_in_progress` field, event set/clear, exported `bt_pairing_in_progress`) — already flashed, harmless, documented for future use. The FAP simply stops consulting it.
- Untouched: Path A/B structure, activity reset, Done zombie-kick, manual reset.
- FAP byte size: 69628. Build exit 0, 0 warnings; clang-format clean; mirror `diff -q` passes; both patch files verified byte-identical to a fresh regeneration (FAP-only change). No commits.
## [2026-08-31 17:20] Task: forget-bonds-after-typing

Round-8 FAP-only. Cycle broken: every successful pairing instantly re-bonded the macOS HID daemon, which squatted the link and killed browser bring-up. Fix: `bt_forget_bonded_devices(app->bt)` inserted in `app_typing_step()`'s BLE-typing completion -> Waiting transition, AFTER `bt_disconnect` and BEFORE `furi_hal_bt_start_advertising`. The OS-level HID bond is single-session; wiping keys storage makes the daemon's auto-reconnect fail auth (stale LTK) so macOS backs off. Async (BtMessageTypeForgetBondedDevices -> bt_keys_storage_delete), no wait needed. The browser's part-2 bond is created later and survives. One daemon rebond dialog (BOND_LOST -> allow_rebond) is acceptable — cancel once.

- Verified before use: `bt_forget_bonded_devices` is `Function,+` (api_symbols.csv:808) and declared in bt.h (already included by the FAP).
- BLE-typing completion path ONLY — not USB typing, abort, or exit paths.
- FAP byte size: 70033. Build exit 0, 0 warnings; clang-format clean; mirror `diff -q` passes; both patch files unchanged (verified vs fresh regeneration). No commits.
## [2026-09-01 14:19] Task: bootstrap-stale-x-fix

Reset stale module-level BluetoothDevice state at the top of `web/bootstrap-ble.js` click handler by replacing `b[I]=1;let n=-1` with `b[I]=1,x=k=r=0;let n=-1`. This forces every click to start clean so the `getDevices` probe loop and `requestDevice` fallback can run again instead of silently reusing a dead `x`. Syntax verified with `node --check`. New byte size: 2048 (+8 bytes).
## [2026-09-01 14:40] Task: serial-only-ble-profile

Structural fix for the macOS HID-daemon squatter: HIDS now exists ONLY during the BLE Deploy typing window. Resting profile is a new serial-only GATT profile; the daemon never bonds/squats outside typing, so no kick metronome is needed against it (watchdogs kept as harmless safety nets).

- New `lib/ble_profile/extra_profiles/airbridge_serial_profile.c`: `ble_profile_airbridge_serial` template = Battery + AirBridge DIS + AirBridge serial, NO HIDS. GATT budget battery(8)+DIS(9)+serial(12)=29 <= 68. Same GapConfig as the composite (identity-driven name/MAC/appearance/mfg data, adv service = serial UUID, bonding + PinCodeVerifyYesNo, 7.5-45 ms CI), so advertising/pairing/browser behavior is unchanged. Sources are GlobRecursive-picked; template declared in the existing `airbridge_profile.h` (already `Header,+` + SDK_HEADERS) so no new header plumbing. NOTE: appearance stays keyboard (0x03C1 from identity) — hardware watch item if the daemon still shows interest without HIDS.
- bt.c: `bt_profile_is_airbridge` now matches BOTH templates (weak extern added), so the raw-serial hook, TX path, and connect/disconnect CCCD callback wiring work identically on the serial-only profile. api_symbols.csv: one new `Variable,+,ble_profile_airbridge_serial`; API check passed WITHOUT bump — 87.4 stayed.
- FAP runtime switching reuses the existing `bt_profile_start` mechanism (BtMessageTypeSetProfile -> bt_change_profile -> furi_hal_bt_change_app: full reinit + auto-advertise). New `app_ble_profile_switch(app, composite)` helper: idempotent via new `ble_composite_active` field; bt_disconnect + bounded 500 ms wait for the disconnect event FIRST (same crash class as app_restore_ble — late disconnect events into freed services furi_crash), then bt_profile_start, then re-latch hids_adv_active=false (gap_init resets advertise_hids on every switch!) and app_set_hids_adv(true) only on the composite direction. Bonds persist across switches: bt_change_profile reloads bt_keys_storage from SD into the new gap_init.
- Switch points: startup installs serial-only (app_configure_ble); BLE Deploy prompt-OK installs composite in app_start_typing AFTER bootstrap load (failure -> "BLE PROFILE ERROR"); Waiting entry switches back (bt_disconnect -> bt_forget_bonded_devices -> switch -> start_advertising; the wipe is queued before the switch so keys storage is deleted before bt_change_profile reloads it — round-8 semantics preserved); abort (app_abort_typing, guarded by app->running so exit teardown skips it) and BLE typing/prompt errors (app_show_error) also switch back so composite never leaks onto Bridge/Error.
- Typing now waits for the link: app_typing_step holds emission while `!ble_connected || bt_pairing_in_progress(bt)` (first FAP use of the round-6 export) — the HID pairing ceremony happens during typing prep after prompt-OK, and burning keystrokes into the 5x20 ms retry budget pre-bond would error the session. Input stays serviced, BACK aborts instantly. BLE prompt text updated: "Press OK, then pair / \"HP 725 K+M\" in BT settings".
- CRASH-CLASS GUARD: the serial-only profile has no HID service, so `ble_profile_airbridge_kb_report` on it hits `furi_check(profile->config == ble_profile_airbridge)`. Guarded everywhere via ble_composite_active: the switch-to-serial-only clears release_all_pending (link teardown releases host-side key state anyway), and both release-all service sites (main loop + exit drain) additionally require ble_composite_active.
- app_set_hids_adv(true) call sites at prompt entry / typing start / startup were removed (serial-only profile must never advertise the HIDS UUID); the function survives, used only by the switch helper on the composite direction. Exit-teardown app_set_hids_adv(false) kept (harmless).
- Runtime-switch constraint discovered: `bt_profile_start` while a link is up would reinit GAP mid-connection; always bt_disconnect + bounded ble_connected wait first. Also bt_forget_bonded_devices is fire-and-forget but FIFO-ordered before the SetProfile message, so wipe-then-switch sequencing is free.
- Repo-state note: the new profile file is untracked; `git add -N` (intent-to-add, NOT a commit) was applied so `git diff c9ab2b68` includes it in the regenerated patch. Future regenerations must keep that intent-to-add entry (or the file silently drops out of airbridge-firmware.patch).
- Verification: `./fbt` exit 0; FAP build exit 0, 0 warnings, APPCHK pass; clang-format --dry-run --Werror clean on all 12 touched C/H files (format -i run first on the 4 edited ones). Mirror `diff -q` passes. Patches regenerated vs c9ab2b68: main 30 files (includes new-file diff), api patch has the new Variable. No commits; no flashing/deploying.

## [2026-09-01 15:33] Task: identity-address-split

Fixed the fatal macOS GATT-cache flaw in the runtime serial-only/composite profile switch by giving each stable GATT database its own stable Bluetooth identity.

- `AirbridgeApp` now owns both identities for the full cross-thread profile-param lifetime: `ble_identity` remains the config-loaded data identity, and `ble_typing_identity` is copied from it after config validation/fallback so name, appearance, manufacturer data, and DIS remain identical.
- Address derivation toggles bit 2 (`0x04`) of `mac_address[5]`. `GapConfig.mac_address` is written to `CONFIG_DATA_PUBADDR`, and `CFG_IDENTITY_ADDRESS` is `GAP_PUBLIC_ADDR`; the six-byte array is controller little-endian, so index 5 is the most-significant on-air octet. The rule maps an aired data prefix `02` to typing prefix `06` (the source default `01` maps to `05`), always differs, and preserves the octet's upper two bits if random-static semantics are ever introduced.
- `app_ble_profile_switch(app, composite)` now passes `ble_typing_identity` with `ble_profile_airbridge` and the original `ble_identity` with `ble_profile_airbridge_serial`. Startup still installs serial-only with the data identity. Neither address ever changes its GATT content, so macOS can cache both safely.
- Preserved unchanged: bonded-link typing hold (`ble_connected && !bt_pairing_in_progress`), round-8 bond wipe at Waiting entry, profile-switch disconnect guard, `ble_composite_active` crash guards, all watchdogs/kicks/manual reset, BLE prompt wording, HIDS advertising re-latch, and the entire USB Deploy path.
- The new serial-only profile file remains intent-to-add (`git status` reports ` A`) so it is retained in `airbridge-firmware.patch`; no profile/UUID/wire-protocol source was changed in this task.

## [2026-09-01 16:21] Task: revert-to-single-identity

User rejected the two-identity/runtime-profile design: Pocket AirBridge must expose one always-installed composite BLE identity (`HP 725 K+M`) using the single address loaded from config.

- Removed the serial-only `ble_profile_airbridge_serial` implementation, declaration, API export, intent-to-add index entry, and bt.c dual-template matching. `bt_profile_is_airbridge` is composite-only again.
- Removed the FAP's `app_ble_profile_switch`, `ble_composite_active`, `ble_typing_identity`, address derivation, switch call sites, and now-unnecessary keyboard/release-all guards. Startup installs `ble_profile_airbridge` with `ble_identity`; the bounded disconnect wait in `app_restore_ble` remains intact.
- Restored the HIDS advertising lifecycle through `app_set_hids_adv`: enabled for the BLE Deploy prompt/typing window and disabled on Waiting entry, abort, errors, prompt exit, startup latch, and teardown. HIDS remains in GATT for the app lifetime.
- Kept all rounds 1-8 work: honest connect/disconnect status and GAP teardown, pairing-state export, CCCD subscription tracking, both squatter-watchdog paths with RX activity reset, Done zombie-kick, manual reset, bridge/watchdog/Waiting pump, stream timestamping, and `bt_forget_bonded_devices` at BLE Waiting entry.
- Kept today's identity-independent typing fixes: hold while `!ble_connected || bt_pairing_in_progress`, `typing_link_ready_tick`, the 2500 ms link settle, and its reset in `app_start_typing`.
- Accepted trade-off by user decision: because HIDS is always present in GATT under one identity, the macOS HID daemon can re-arm on later browser pairings. The 15 s subscription-aware squatter kick, activity reset, bond wipe after typing, and manual reset remain the mitigation rather than maintaining two BLE identities.
- Verification: full `./fbt` and FAP build passed with API 87.4 and zero warnings; clang-format Werror and compile-database clangd checks passed; FAP mirror is byte-identical; both patches were regenerated from `c9ab2b68`, and the deleted serial profile is absent from the firmware patch. No commit, flash, deploy, web change, or hardware action was performed.
## [2026-09-01 16:25] Task: final-single-identity-working-set

HARDWARE-VERIFIED END-TO-END (16:13-16:19): BLE Deploy typing (clean, first chars intact) -> Waiting -> bootstrap picker/bonded connect -> 32249 B stream -> checksum/inflate -> page replace -> part-3 app connect -> USB WebHID connect -> SAS 642998 match -> bidirectional encrypted text (hash verified) -> counters 26/26/0/0 -> protocol harness 35/35. The original part-3 regression is closed on the final build.

The session ran three architectures before landing:
1. Serial-only resting profile + runtime switching (14:40): FAILED — macOS caches GATT per bonded address; a runtime GATT change (HIDS in/out) on one address leaves the host serving a stale table (paired-but-unconnectable keyboard).
2. Identity/address split (typing 06:… vs data 02:…): WORKED mechanically but the user rejected two paired devices / two pairing ceremonies — "it must be just one hid".
3. FINAL single-identity always-advertise set (user-directed):
   - FAP: HIDS stays in adv data for the whole app lifetime (app_set_hids_adv(true) at startup; all prompt/typing/abort/error toggles REMOVED — this reverses the July stealth window by user decision). The keyboard is discoverable in host BT settings with zero Flipper navigation.
   - gap.c: gap_set_adv_hids no-change early-out so periodic re-arming (bridge watchdog + Waiting pump call furi_hal_bt_set_adv_hids(true) every tick) is free — kills the startup race where the first HIDS command could be lost (hardware: serial-only adv after boot until this fix).
   - FAP: round-8 bond wipe REMOVED from Waiting entry — with one identity the typing bond doubles as the browser's data bond; wiping leaves the Mac's bond record pointing at dead keys and every later connect fails with "Connection attempt failed" (hardware-proven). Squatter handled by the 15 s kick only.
   - FAP: Waiting zombie-kick (90 s, ble_last_rx_tick == 0, kicks even SUBSCRIBED links) — a Chrome zombie (gatt.disconnect() does not drop the OS link) that subscribed but never wrote 0x42 wedged advertising forever (hardware: 18+ min no-adv).
   - FAP: Done zombie-kick grace 4000 ms (done_since stamp) — Done is entered when the last chunk is QUEUED; the immediate kick dropped the unsent BLE tail and the bootstrap showed [gattserverdisconnected] + "Transfer failed" after a fully-sent stream (hardware). The grace lets notifications flush; the bootstrap's own confirmed-disconnect (2500 ms) usually lands first.
   - KEPT from today: typing-hold (no keystrokes until ble_connected && !bt_pairing_in_progress) and BLE_TYPE_LINK_SETTLE_MS 2500 (macOS HID daemon subscribes report CCCDs ~1.2 s after pairing; earlier keystrokes are silently dropped — hardware: first 24 bootstrap chars lost starting at ',d=document').
   - Bootstrap-ble.js: x=k=r=0 reset per click (2048 B) — stale module-level device made retries silently reuse a dead object, skipping getDevices AND the picker until page reload.

Remaining known rough edges (accepted for now): part-3 picker needs a kick window when the bonded daemon squats (worked first try this run); Chrome getDevices grants go stale across pairings ("no longer in range") so the picker is the reliable path; USB deploy flow not retested this session (fresh assets deployed to SD: bootstrap.js 1196, app-usb.html.gz 31898).

## [2026-09-01 23:25] Task: usb-deploy-picker-timeout-fix

USB Deploy regression reproduced and fixed on hardware. Root cause was entirely in `web/bootstrap.js`: the `self.ABT || 8000` timeout promise was created before `navigator.hid.requestDevice()`, so the native picker and human selection delay consumed the transfer deadline. If the picker took longer than eight seconds, its promise was already rejected before WebHID opened; after selection the bootstrap sent `0x42`, immediately caught the stale timeout, closed HID, and left the Flipper serving into a closed host endpoint.

- Fix: construct the transfer promise only after `requestDevice()` and `x.open()`, attach `oninputreport`, then arm the same 8 s timeout immediately before sending `0x42`. The deadline now measures transfer only, not user interaction.
- Toggle proof: a VM driver with a 20 ms picker delay and a 5 ms transfer timeout failed pre-fix with an unhandled rejection `2`; the same driver passed after the timer move and loaded the synthetic app payload.
- Build proof: `node --check web/bootstrap.js` passed; `tools/build_bundle.py` passed; typed payload remains exactly 1196 characters. Generated `dist/` outputs were restored because the pre-existing uncommitted transport changes otherwise regenerate unrelated bundles.
- Device deploy: `/ext/apps_data/pocket_airbridge/bootstrap.js` replaced and size-verified at 1196 bytes. Firmware and FAP were not rebuilt, flashed, or replaced.
- Hardware toggle proof: opened the real WebHID picker and deliberately waited more than 12 seconds (longer than the old 8 s deadline) before selecting `HP Wireless Keyboard and Mouse`; the 31,926-byte USB bundle then transferred, checksum/inflate succeeded, and `https://blank.org/` replaced with `Pocket AirBridge — USB Chat`.
- Regression evidence: protocol harness remains `PASS: 35/35 protocol tests passed`; loaded USB app produced zero browser console errors. No commits or pushes.
